# Original archives this project was unpacked from. Kept on disk as a
# safety net, never committed — the repo root is now the project itself.
.backup/

# Vercel
.vercel
.vercel/

# OS
.DS_Store
Thumbs.db
desktop.ini
